// TODO: Delete. What even is tihs?
using System;
using UnityEngine;

/// <summary>
/// Entity database
/// </summary>
[Obsolete("What")]
public class Db : Singleton<Db>{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
